<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iqiny.example.sillyactiviti.admin.modules.sys.dao.SysUserDao">
	
	<!-- 查询用户的所有权限 -->
	<select id="queryAllPerms" resultType="java.lang.String">
		select t1.perms
		from sys_menu t1
		left join sys_role_menu t2 on t2.menu_id = t1.menu_id
		left join sys_user_role t3 on t3.role_id = t2.role_id
		<where>
			t1.perms is not null and t1.perms  != ''
			<if test="userId!=null">
				and t3.user_id = #{userId}
			</if>
		</where>
		group by t1.perms
	</select>
	
	<!-- 查询用户的所有菜单ID --> 
	<select id="queryAllMenuId" resultType="java.lang.String">
		select distinct rm.menu_id
		from sys_user_role ur
		LEFT JOIN sys_role_menu rm on ur.role_id = rm.role_id
		where ur.user_id = #{userId}
	</select>

</mapper>